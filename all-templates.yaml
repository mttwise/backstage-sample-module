apiVersion: backstage.io/v1alpha1
kind: Location
metadata:
  name: all-templates
  description: A collection of all Backstage templates
spec:
  targets:
    - ./sample-module-with-acdp/template.yaml
    - ./sample-module-with-owner-hosted-cfn/template.yaml
